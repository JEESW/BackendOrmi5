import java.util.OptionalDouble;
import java.util.stream.IntStream;

public class while_1_240604 {
  public static void main(String[] args) {
    //    int i = 1;
    //    while (i <= 5) {
    //      System.out.println("현재 i의 값은 = " + i);
    //      i++;
    //    }

    //    int i = 10;
    //    while (i >= 0) {
    //      System.out.println("현재 카운트 : " + i);
    //      i--;
    //    }
    //    System.out.println("카운트 종료");

    //    int n = 1;
    //    int sum = 0;
    //    while (n < 101) {
    //      sum += n;
    //      n++;
    //    }
    //    System.out.println(sum);

    //    int[] arr = {5,2,9,1,7,4,6,3,8};
    //    double aver = 0;
    //    int i = 0;
    //    while (i < arr.length) {
    //      aver += arr[i];
    //      i++;
    //    }
    //    System.out.println("평균값은 : " + (int)(aver / arr.length));

    //    int i = 0;
    //    while (i <= 300) {
    //      if (i % 3 == 0) System.out.println(i);
    //      i++;
    //    }

    //    int[] numbers = {10, 5, 8, 20, 3, 15, 9, 2};
    //    int max = 0, idx = 0;
    //
    //    while (idx < numbers.length) {
    //        max = (numbers[idx] > max) ? numbers[idx] : max;
    //        idx++;
    //    }
    //
    //    System.out.println(max);

    //    int[] numbers = {-4, -2, 9, -7, 5, 1, -3, 6, -1, 8};
    //    int idx = 0, sum_mi = 0, sum_pl = 0;
    //
    //    while (idx < numbers.length) {
    //        if (numbers[idx] > 0)   sum_pl += numbers[idx];
    //        else    sum_mi += numbers[idx];
    //        idx++;
    //    }
    //    System.out.println(sum_pl);
    //    System.out.println(sum_mi);

    //    int[] numbers = {-4, -2, 9, -7, 5, 1, -3, 6, -1, 8};
    //    int posNum = IntStream.of(numbers).filter(num -> num > 0).sum();
    //    int negNum = IntStream.of(numbers).filter(num -> num < 0).sum();
    //    System.out.println(posNum);
    //    System.out.println(negNum);
  }
}
